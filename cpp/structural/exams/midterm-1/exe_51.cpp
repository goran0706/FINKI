/*
    =============================================================
    TASK 3
    =============================================================
    Given an integer n, print a rectangular frame of size n x n where the top and bottom rows consist
    entirely of '+' characters, and each middle row has '+' on both ends with '-' characters in between.

    Input: 5
    Output:
    +++++
    +---+
    +---+
    +++++
    =============================================================
*/
