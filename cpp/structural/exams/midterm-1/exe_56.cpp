/*
    =============================================================
    TASK 8
    =============================================================
    Given two integers a and b, find all numbers in the range [a, b] for which the reversed middle digits
    (all digits except the first and last) are divisible by the sum of the first and last digit,
    and that reversed middle value is greater than 0.

    For each such number print it in the format: i -> (reversed_middle == (last + first) * k)
    
    At the end print the total count of such numbers.

    Input: 1000 2000
    Output:
    (matching numbers in format above)
    count
    =============================================================
*/