/*
    =============================================================
    TASK 2
    =============================================================
    Given two integers a and b, find and print all numbers in the range [a, b] for which the product
    of the middle digits (all digits except the first and last) is divisible by the number formed by concatenating
    the first and last digit, and that product is non-zero.

    For each such number print it in the format: i -> (middle_product == first_last * k)

    At the end print the total count of such numbers.

    Input: 100 200
    Output:
    (matching numbers in format above)
    count
    =============================================================
*/
