# Algorithms To Solve Usual Tasks

## Numbers

- Find the most significant digit
- Find the min most significant digit
- Find the max most significant digit
- Find the least significant digit
- Find the min least significant digit
- Find the max least significant digit
- Count the digits of a number
- Count the occurrences of a digit in a number
- Sum the digits of a number
- Sum the digits of a number using pointers
- Sum odd digits of a number
- Sum even digits of a number
- Replace digits of a number
- Reverse digits of a number
- Transform a digit with its first letter
- Check if a number is a prime number
- Check if a number is a palindrome
- Check if a number is a perfect number
- Find the Greatest Common Divisor (GCD) of two numbers
- Find the Least Common Multiple (LCM) of two numbers
- Find the factorial of a number (the product of all positive integers from 1 up to that number)
- Check if a number is an Armstrong number (the sum of its digits cubed/powered equals the number)
- Check if a number is a Harshad number (a number that is exactly divisible by the sum of its digits)
- Check if a number is a Strong number (the sum of the factorials of its digits equals the number)

## Arrays

- Transform array even first (reverse order), odd last (original order)
- Transform array even first (original order), odd last (reverse order)
- Transform array odd first (reverse order), even last (original order)
- Transform array odd first (original order), even last (reverse order)
- Transform array positive first (reverse order), negative last (original order)
- Transform array positive first (original order), negative last (reverse order)
- Transform array negative first (reverse order), positive last (original order)
- Transform array negative first (original order), positive last (reverse order)
- Transform array zeros first (reverse order), non-zeros last (original order)
- Transform array zeros first (original order), non-zeros last (reverse order)
- Transform array non-zeros first (reverse order), zeros last (original order)
- Transform array non-zeros first (original order), zeros last (reverse order)
- Transform array multiples of 3 first (reverse order), others last (original order)
- Transform array multiples of 3 first (original order), others last (reverse order)
- Transform array others first (reverse order), multiples of 3 last (original order)
- Transform array others first (original order), multiples of 3 last (reverse order)
- Transform array numbers by replacing digit 5 with 2 print first 5 numbers sorted
- Transform array numbers by replacing digit 9 with 7 print first 5 numbers sorted
- Sum array elements
- Sum array elements using pointers
- Sum array elements using pointers starting from x position
- Sum array elements using recursive function
- Sum array elements using recursive function starting from x position
- Product of array elements
- Product of array elements using pointers
- Product of array elements using pointers starting from x position
- Product of array elements using recursive function
- Product of array elements using recursive function starting from x position
- Check if array elements are strictly incremental
- Check if array elements are strictly decremental
- Check if array elements are non-decreasing
- Check if array elements are non-increasing
- Check zigzag array elements (peaks and valleys)
- Check how many times elements are changing from decreasing to increasing
- Find the min element
- Find the max element
- Find the second min element
- Find the second max element
- Find the longest palindrome
- Find the shortest palindrome
- Find the longest subarray with sum equal to K
- Find the shortest subarray with sum greater than K
- Find the smallest element that occurs odd number of times
- Find the smallest element that occurs even number of times
- Find and count pairs with given sum
- Sorting using bubble sort
- Sorting using selection sort
- Rotate array elements to the right by K positions using reverse algorithm
- Rotate array elements to the left by K positions using juggling algorithm
- Rotate all zeros to the end maintaining relative order of non-zeros
- Remove duplicates from sorted array in-place
- Generate an array where each element is the furthest difference from arithmetic middle of a matrix row
- Generate an array where each element is the furthest difference from arithmetic middle of a matrix col
- Generate an array where each element is the furthest difference from geometric mean of a matrix row
- Generate an array where each element is the furthest difference from geometric mean of a matrix col

## Matrix

- Read the matrix elements from standard input
- Fill all matrix elements with a specific value or pattern
- Transform the entire matrix using a global modification rule
- Print elements on the main diagonal
- Print elements above the main diagonal
- Print elements below the main diagonal
- Print elements on the opposite diagonal
- Print elements above the opposite diagonal
- Print elements below the opposite diagonal
- Sum elements on the main diagonal
- Sum elements above the main diagonal
- Sum elements below the main diagonal
- Sum elements on the opposite diagonal
- Sum elements above the opposite diagonal
- Sum elements below the opposite diagonal
- Print even rows from left to right
- Print odd rows from right to left
- Count rows containing X consecutive Y times
- Count rows containing the element one at least three times
- Store the element furthest from its row average into a separate array
- Transform each element to one, negative one, or zero based on its relation to X
- Print elements forming the Z pattern
- Print elements forming the N pattern
- Print elements forming the X pattern
- Print elements forming the + pattern
- Matrix Z Pattern (Cumulative Sums)
- Matrix N Pattern (Cumulative Sums)
- Matrix X Pattern (Cumulative Sums)
- Matrix + Pattern (Cumulative Sums)
- Print elements of a submatrix
- Print elements of a submatrix forming the Z pattern
- Print elements of a submatrix forming the N pattern
- Print elements of a submatrix forming the X pattern
- Print elements of a submatrix forming the + pattern
- Print elements row-by-row, traversing odd rows left-to-right and even rows right-to-left
- Print elements row-by-row, traversing even rows left-to-right and odd rows right-to-left
- Print elements column-by-column, traversing odd columns top-to-bottom and even columns bottom-to-top
- Print elements column-by-column, traversing even columns top-to-bottom and odd columns bottom-to-top
- Print the sum of all 2x2 submatrices and find the maximum 2x2 sum
- Find the minimum element and transform the matrix
- Find the largest diamond and sum of all of its elements
- Rotate matrix by x places to the right
- Rotate matrix by y places to the left
- Rotate matrix by 90 degrees to the right
- Rotate matrix by 90 degrees to the left
- Rotate matrix by 180 degrees to the right
- Rotate matrix by 180 degrees to the left
- Rotate matrix right half bellow the matrix
- Transform rows based on sum 1 if sum > x; -1 if sum < x; 0 if sum == x
- Transform matrix middle row element based on abs difference between row left and right half
- Transform matrix middle col element based on abs difference between col left and right half

## String

- Trim white spaces at start and end of a string
- use built-in `strstr`
- use built-in `strcmp` or `strncmp`
- use custom substring function
- use custom substring function and count occurrences/frequency
- Find a first and last digit in a string and print the substring between
- Print a substring between ch1 and ch2
- Print substring between x and y position
- Check if string is a palindrome
- Find the longest palindrome
- Find palindrome that has a special character
- Find string with most adjectives
- Caesar cipher
- Replace x character with y
- Compress a string replace consecutive letters with their count
- Substring count
- Consecutive substring count
- Consecutive vowels count
- Consecutive consonants count
- Read two characters then print the substrings between the characters.
- Find the longest string that contains least 2 digits then print the substring between the digits
- Find the longest string that contains least 2 digits then print the substring between and including digits

## Recursion

- Recursive Sequence (Step −5)
- Recursive Sequence (Step −3)
- Recursive Punctuation Search
- Recursive Uppercase Search
- Recursive Positive Digits Count
- Recursive Negative Digits Count
- Recursive max digit of a number
- Recursive Max number
- Recursive Caesar cipher
- Recursive Palindrome
- Recursive Triangle
- Recursive Pyramid Print
- Recursive Print odd digits starting from lsd, even starting from msd, print sum of odd
- Recursive Print odd digits starting from lsd, even starting from msd, print sum of even

## Pointers

- Sum array elements using pointer arithmetics
- Print array elements using pointer arithmetics

## File Stream (fstream)

- Read from a file
- Write to a file
