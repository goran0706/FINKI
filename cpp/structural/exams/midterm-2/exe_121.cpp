/*
    =============================================================
    EXERCISE 27
    =============================================================
    Read M (rows) and N (columns), then an M×N matrix.
    Find the column whose sum of elements is minimal.
    Print the 0-based index of that column.
    If multiple columns share the minimum sum, print the index of the first such column.

    Example:
    Input : 2 3
    3 1 4
    1 5 9
    Output: 0   (col0=4, col1=6, col2=13 → min is col0)
*/
