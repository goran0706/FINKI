/*
    =============================================================
    EXERCISE 32
    =============================================================
    Read lines from standard input until EOF.
    Consider only lines with more than 4 characters.
    For each such line, count the number of characters that appear exactly once in that line (case-insensitive).
    Print the original line (with original casing) that has the highest such count. If two lines tie, the last one wins.

    Example:
    Input :
    aabbcc
    abcdef
    xyzabc
    Output: abcdef
*/