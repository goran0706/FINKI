/*
    =============================================================
    TASK 4
    =============================================================
    Read triplets of integers (a, b, c) until end of input.
    For each triplet, count how many times digit c appears at every second position from the right
    (positions 1, 3, 5...i.e. the tens, thousands, etc. digits) in a and b separately.
    Print whichever of a or b has more such occurrences.
    If b has strictly more, print b; otherwise print a.

    Example:
    a=123, b=231, c=2 -> 1 vs 1 -> print a: 123
    =============================================================
*/
