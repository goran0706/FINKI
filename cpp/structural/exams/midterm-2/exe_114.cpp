/*
    =============================================================
    EXERCISE 20
    =============================================================
    A "Strong number" is a number equal to the sum of the factorial of its digits  (e.g. 145 = 1! + 4! + 5!).
    NOTE: the factorial used here is the product of the last digit of each step:
        factorial(a) = (a%10) * factorial(a-1),
        factorial(0) = 1.

    Read N, then N integers.
    Print "Strong numbers:" followed by each strong number on its own line.

    Example:
    Input : 4
    1 2 145 40585
    Output:
    Strong numbers:
    1
    2
    145
*/
