/*
    =============================================================
    EXERCISE 29
    =============================================================
    Read words (up to 30 chars) from standard input, one per line, until EOF.
    Print only those words that are "vowel-palindromes":
    a word where every vowel at position i from the start has the same vowel (case-insensitive)
    at position i from the end, whenever either of the two mirrored characters is a vowel.
    (Consonant-consonant mirror pairs are ignored.)

    Example:
    Input :
    level
    hello
    civic
    Output:
    level
    civic
*/
