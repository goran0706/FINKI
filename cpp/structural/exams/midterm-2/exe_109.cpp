/*
    =============================================================
    EXERCISE 15
    =============================================================
    Read words from standard input one by one (until EOF).
    For each word, print only its alphabetic characters on a new line.
    Skip the line entirely if the word contains no alphabetic characters at all.

    Input:  a stream of whitespace-separated tokens
    Output: one filtered word per line (only [a-zA-Z] chars)

    Example:
    Input : h3ll0  w@rld  123
    Output:
    hll
    wrld
*/
