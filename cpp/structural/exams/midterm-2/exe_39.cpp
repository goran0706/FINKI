/*
    =======================
    Task 6
    =======================

    Read a binary matrix (m < 100, n < 100).
    Then read the coordinates of one element.

    If the element at the given position is 0, it becomes 1 and all zeros
    are filled in all four directions until a 1 or a border is reached.

    If the element is already 1, the matrix remains unchanged.

    Print the resulting matrix.
*/