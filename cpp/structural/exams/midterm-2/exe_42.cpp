/*
    =======================
    Task 9
    =======================

    Define a parity complement of a number.

    Read unknown number of integers (max 100) until a non-number is entered.

    For each number compute its parity complement using a recursive function parcom(int a).

    Print the smallest 5 parity complements in ascending order.
    If fewer than 5 numbers are entered, print all.

    Global variables are forbidden.
*/
