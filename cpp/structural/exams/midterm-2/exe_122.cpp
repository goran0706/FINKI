/*
    =============================================================
    EXERCISE 28
    =============================================================
    Read lines from standard input until EOF.
    For each line:
    1. Print all alphabetic characters of the line (no spaces, no digits, no punctuation).
    2. Then print the sum of all numbers (consecutive digit sequences are treated as a single integer) found in the line.
    If no digits exist, print 0.
    Both items are printed on the same line.

    Example:
    Input :
    abc12def34
    hello
    Output:
    abcdef46
    hello0
*/
