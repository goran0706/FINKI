/*
    =============================================================
    EXERCISE 18
    =============================================================
    Read lines from standard input until EOF.
    For each line compute the ratio  digits / letters.
    (Ignore lines that contain no alphabetic characters.)
    At the end, print the line that has the highest digit-to-letter ratio. If two lines tie, the last one wins.

    Example:
    Input :
    abc
    a1b2c3
    xx99
    Output: xx99
*/
