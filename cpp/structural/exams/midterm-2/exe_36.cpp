/*
    =======================
    Task 3
    =======================

    Let A = {a1, a2, .. a_n} be an array of integers.
    The size of the array n and the elements are read from SI.

    Transform the array so each element of the original array is replaced with the number of appearances
    of the least significant digit (rightmost) in the number itself.

    The counting of a digit in a number should be implemented in a separate recursive function.

    Example:

    Input:
    5
    1 11 1121 111222112 22222

    Output:
    1 2 3 4 5
*/
