/*
    =======================
    Task 11
    =======================

    Read a string (max 100 chars) containing letters and digits.

    Print the characters between the first and second digit (inclusive).

    If there is only one digit, print everything from that digit to the end.

    If there are no digits, print:
    No digits
*/
