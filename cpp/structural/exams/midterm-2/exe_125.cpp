/*
    =============================================================
    EXERCISE 31
    =============================================================
    Read lines from standard input until EOF.
    For each line compute the ratio  uppercase / lowercase.
    Print the ratio (2 decimal places) followed by a space and the original line.
    At the end, print the 0-based index of the line with the highest uppercase-to-lowercase ratio.

    Example:
    Input :
    Hello
    WORLD
    Output:
    0.25 Hello
    5.00 WORLD
    1
*/
