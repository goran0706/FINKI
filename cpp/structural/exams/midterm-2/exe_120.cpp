/*
    =============================================================
    EXERCISE 26
    =============================================================
    Read M (rows) and N (columns), then an M×N matrix.
    For each row, collect all elements that appear exactly once in that row (unique elements).
    Print all such unique elements from all rows separated by spaces.
    If no unique element exists in any row, print "N".

    Example:
    Input : 2 4
    1 2 3 2
    4 5 5 6
    Output: 1 3 4 6
*/
